<%= render @assignment %>

<% if @assignment.worker.nil? %>
  <%= link_to 'Accept Task!', {:controller => :assignments, :action => :assign_me, :id => @assignment}, {"data-icon" => "button", "id" => "a"}%> 
<% end %>