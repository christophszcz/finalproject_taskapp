
 <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%= render @assignment %>

<%= form_for([@assignment, @review]) do |f| %>
	<div class="field">
			<%= f.label :rating %>
			<%= f.select :rating, [['1'],['2'],['3'],['4'],['5']]%>
	</div>
	<div class="field">
			<%= f.label :Comment %><br>
			<%= f.text_area :comment %>
	</div></br>

	<div class="action">
		<%= f.submit %>
	</div>
<% end %>